{
  "name": "cps596web",
  "version": "1.0.0",
  "description": "Survey web app for ranking generative ai images",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@firebase/app-compat": "^0.2.24",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "firebase-admin": "^9.8.0"
  },
  "engines": {
    "node": "v14.21.3"
  }
}
